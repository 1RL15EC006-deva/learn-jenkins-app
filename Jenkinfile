pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                dir('/Users/ddurg/AppData/Roaming/npm') {
                    bat 'npm --version'
                    bat 'newman -version'
                }
            }
        }
    }
}